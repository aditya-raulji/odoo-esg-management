'use client';
import Card from '@/components/ui/Card';

export default function CustomReportBuilder() {
  return (
    <Card>
      <h2 className="text-xl font-bold text-[var(--text)] font-space-grotesk mb-2">Custom Report Builder</h2>
      <p className="text-sm text-[var(--muted)]">Custom filtering and generation will be implemented here.</p>
    </Card>
  );
}
